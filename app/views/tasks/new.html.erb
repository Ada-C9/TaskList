<%= form_for @task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <!-- <%= f.submit %> -->
  <%= f.label :completion_date %>
  <%= f.date_field :completion_date %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit %>
<% end %>
