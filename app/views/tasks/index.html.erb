<h2>Daily To-Do</h2>

<ul class="task-list">
    <% @tasks.each do |task| %>
      <li>
        <%   if task.status == "DONE" %>
        <del>
          <strong>
            <%= link_to(task.name, task_path(task)) %>
        </strong>
      </del>
        <%   else  %>
        <strong>
          <%= link_to(task.name, task_path(task)) %>
        </strong>
        <%   end  %>
        <br />
        <em><%= task.description %></em>
        <br />
        <% if task.status == "IN PROGRESS"%>
            <%= link_to("Done", complete_task_path(task), :data => {:method => "post"}) %>
        <% else %>
            <%= link_to("Mark In Progress", complete_task_path(task), :data => {:method => "post"}) %>
        <% end %>
        <%= link_to("Edit", edit_task_path(task)) %>
        <%= link_to("Delete", delete_task_path(task), :data => {:confirm => "Are you sure you want to delete this task?",
        :method => "delete"}) %></p>
      </li>
    <% end %>
</ul>
