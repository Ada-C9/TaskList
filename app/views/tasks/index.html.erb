<h2>All TASKS</h2>
<ul><% @tasks.each do |task| %>
  <li><%= link_to task.title,task_path(task.id), class: 'title_link' %>
    : <%= task.description %> <%= task.completion_date %>
  </li>

  <% end %>
</ul>
