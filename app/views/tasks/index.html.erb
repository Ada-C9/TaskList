<h1>All Tasks</h1>

<ul id="all-tasks">
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.name, task_path(task.id) %>
    </li>
    <li id="description">
      <%= task.description %>
    </li>
  <% end %>
</ul>
