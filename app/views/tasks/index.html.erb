<h1>Tasklist Application</h1>

<strong><h2>Tasks</h2></strong></p>

<ul class="tasks">
  <% @tasks.each do |task| %>
  <li>
    <p> <strong> <%=link_to task.name, task_path(task) %></strong>
  </li>
  <li>
    <%=link_to 'Edit', edit_task_path(task)%>
  </li>
  <% end %>
</ul>
