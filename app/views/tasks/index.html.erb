<h2> Good morning Wini!</h2>
<p> Your tasks for this week are:</p>
<p>
<ul>
<% @tasks.each do |task|%>

  <li>
    <%= link_to task.name, task_detail_path(task.id) %>
  </li>
  <%end%>
</ul>
</p>

<%= link_to"Add new task", new_task_path %>
