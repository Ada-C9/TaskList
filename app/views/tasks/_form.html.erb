<h2 class="my-class"><%= page_title %></h2>

<%= form_for @task do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= f.label :completion_date %>
<%= f.date_field :completion_date, min: Time.now %>

<%= f.submit %>

<% end %>
