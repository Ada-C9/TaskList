<!-- fill this in with the index & new html.erb -->
<h4>
  <% page_title ||= "This is unique page stuffs" %>
  <%= page_title %>
</h4>

<%= form_for @task do |f| %>
  <% f.label :name %>
  <% f.text_field :name %>

  <% f.label :description %>
  <% f.text_field :description %>

  <% f.label :completion_date %>
  <% f.date_field :completion_date %>

  <%= f.submit %>
<% end %>
