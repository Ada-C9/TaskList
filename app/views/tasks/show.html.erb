<h2><%= @task.name %></h2>

<h3>Details: </h3>
<%= @task.description %>

<p>
<ul>
  <li>
    <%= link_to "Edit", edit_task_path %>
  </li>
  <li>
    <%= link_to "Delete", task_path(@task.id), method: :delete, data: { confirm: 'Are you sure?' } %>
  </li>
</ul>
</p>
